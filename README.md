# managant

## Backend

This frontend runs against the real Spring Boot backend (`../managant-backend`).

Create `.env` from `.env.example` and set:

```bash
VITE_API_BASE=http://localhost:8080
```

Then:

```bash
npm run dev
```
